_RWS := rec
(
  isRWS := true,
  isConfluent := false,
  generatorOrder := [a,b,B],
  ordering := "shortlex",
  inverses :=       [a,B,b],
  equations := 
  [
    [(a*b*a*B)^4,IdWord],
    [(a*b)^13,IdWord],
    [b^3,IdWord],
  ]
);
