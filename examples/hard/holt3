_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B],
  inverses := [A,a,B,b],
  equations :=
  [
    [a^7,IdWord],
    [b^5,IdWord],
    [(a*b)^3,IdWord],
    [(B*A*b*a)^2,IdWord]
  ]
);
