_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B],
  inverses := [A,a,B,b],
  equations :=
  [
   [B^2*a*b*a^5*b*a,IdWord],
   [A*b^3*A*b^2*a^2*b^2,IdWord]
  ]
);
