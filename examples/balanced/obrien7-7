_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B],
  inverses := [A,a,B,b],
  equations :=
  [
   [b^2*a^3*B*a*B*a^3,IdWord],
   [a^2*b*A^3*b^2*a*b^4,IdWord]
  ]
);
