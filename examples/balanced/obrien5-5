_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B],
  inverses := [A,a,B,b],
  equations :=
  [
   [B^2*a^2*b*a*b*a^2,IdWord],
   [A*B*A*b^2*a*b^2*a*b^2,IdWord]
  ]
);
