_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B],
  inverses := [A,a,B,b],
  equations :=
  [
   [B*a^2*B*a*b*a^3*b*a,IdWord],
   [A^2*b^2*a*b^3*a*b^2,IdWord]
  ]
);
