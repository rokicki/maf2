_RWS := rec
(
  isRWS := true,
  ordering := "rt_recursive",
  generatorOrder := [a,A,c,C,b,B],
  inverses := [A,a,C,c,B,b],
  equations :=
  [
    [a*a*C*a*c,IdWord],
    [a*c*b^2*A*C*b,IdWord],
    [A*B*a*b*c^3,IdWord]
  ]
);
