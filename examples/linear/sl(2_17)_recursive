#deficiency 0 presentation of sl(2,p) for p=17
_RWS := rec
(
  isRWS := true,
  generatorOrder := [x,X,y,Y],
  ordering := "recursive",
  inverses := [X,x,Y,y],
  equations :=
  [
    [x*x*(Y*X)^3,IdWord],
    [(x*y^4*x*y^9)^2*y^17*x^10,IdWord]
  ]
);
