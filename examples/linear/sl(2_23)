#deficiency 0 presentation of sl(2,p) for p=23
_RWS := rec
(
  isRWS := true,
  generatorOrder := [x,X,y,Y],
  ordering := "shortlex",
  inverses := [X,x,Y,y],
  equations :=
  [
    [x*x*(Y*X)^3,IdWord],
    [(x*y^4*x*y^12)^2*y^23*x^14,IdWord]
  ]
);
