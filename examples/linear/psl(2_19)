#deficiency 0 presentation of sl(2,p) for p=19
_RWS := rec
(
  isRWS := true,
  generatorOrder := [x,y,Y],
  ordering := "recursive",
  inverses := [x,Y,y],
  equations :=
  [
    [x*x*(Y*x)^3,IdWord],
    [(x*y^4*x*y^10)^2*y^19*x^12,IdWord]
  ]
);
