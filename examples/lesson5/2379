#An infinite quotient of the Von Dyck (7,2,3) group 
_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,b,B],
  inverses := [a,B,b],
  equations :=
  [
    [b^3,IdWord],
    [(a*b)^7,IdWord],
    [(a*b*a*B)^9,IdWord]
  ]
);
            
