_RWS := rec
(
  isRWS := true,
  ordering := "recursive",
  generatorOrder := [b,B,a,A,c,C],
  inverses := [B,b,A,a,C,c],
  equations :=
  [
      [b^2 * C * b * c,IdWord],
      [a^2*C*a*c,IdWord],
      [A*B*a*b*c^3,IdWord]
  ]
);
