_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B,c,C],
  inverses := [A,a,B,b,C,c],
  equations :=
  [
    [B*a*b*c*a^2*C,IdWord],
    [a*c*b^2*C*A*b,IdWord],
    [c*B*a*c*b*c*A,IdWord]
  ]
);
