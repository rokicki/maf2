_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B,c,C],
  inverses := [A,a,B,b,C,c],
  equations :=
  [
      [a*b*a^2*b^2,IdWord],
      [b*c*b^2*c^2,IdWord],
      [a*c^2*b*A*c*B,IdWord]
  ]
);
