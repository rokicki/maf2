_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B,c,C],
  inverses := [A,a,B,b,C,c],
  equations :=
  [
      [b^2 * C * b * c,IdWord],
      [a^2*B*a*b,IdWord],
      [B*a*b*c^2*A*c,IdWord]
  ]
);
