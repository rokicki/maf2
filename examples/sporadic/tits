#Tits group Generated by MAF
_RWS := rec
(
  isRWS := true,
  isConfluent := false,
  generatorOrder := [a,b,B],
  ordering := "shortlex",
  inverses :=       [a,B,b],
  equations := 
  [
    [b^3,IdWord],
    [(a*b)^13,IdWord],
    [(a*B*a*b)^5,IdWord],
    [(a*B*a*B*a*b*a*b)^4,IdWord],
    [(a*b*a*b*a*b*a*b*a*B)^6,IdWord]
  ]
);
