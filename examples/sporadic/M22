#Mathieu group M_22
_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,c,C],
  inverses := [a,C,c],
  equations :=
  [
#    [d,(a*c*a*c*a*c*a*c*a*c*c*a*c*a*c*c)^-1*c*(a*c*a*c*a*c*a*c*a*c*c*a*c*a*c*c)],
#    [c,a*c],
    [c^4,IdWord],
    [(a*c)^11,IdWord],
    [(a*c^2)^5,IdWord],
    [(a*C*a*C*a*c*a*c)^3,IdWord],
    [(a*c*a*c*a*C)^5,IdWord]
  ]
);
