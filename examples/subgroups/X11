#Not sure where this came from, but it is a largish finite group of order 2^4*3^10
_RWS := rec
(
  isRWS := true,
  ordering := "recursive",
  generatorOrder := [a,b,B],
  inverses       := [a,B,b],
  equations :=
  [
    [b^6,IdWord],
    [a*b*a*b*a*b*a*b*b*b*b*a*b*a*b*a*b*b*b*b*b*a*b*a*b*a*b*b*b*b*b,IdWord]
  ]
);
