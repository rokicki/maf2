_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B],
  inverses := [A,a,B,b],
  equations :=
  [
    [a^3,IdWord],
    [b^4,IdWord],
    [(a*b)^13,IdWord],
    [(a*b*A*B)^2,IdWord]
  ]
);
