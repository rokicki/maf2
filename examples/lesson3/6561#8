#This is presentation #8 in the series of proposed deficiency zero
#presentations of groups of #order 6561 discussed in "Groups of Deficiency Zero"
_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,A,b,B,c,C],
  inverses := [A,a,B,b,C,c],
  equations :=
  [
    [a*a*C*a*c,IdWord],
    [a*c*b^2*A*C*b,IdWord],
    [A*B*a*b*c^3,IdWord]
  ]
);

