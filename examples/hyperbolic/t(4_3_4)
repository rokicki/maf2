#(4,3,4) triangle group - infinite hyperbolic 
_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,b,c],
  inverses := [a,b,c],
  equations :=
  [
    [(b*c)^4,IdWord],
    [(c*a)^3,IdWord],
    [(a*b)^4,IdWord]
  ]
);
