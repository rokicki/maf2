#5,2,5 triangle group presentation
#    b  +                          
#     / |a                         
#   +---+       
#     c                               
_RWS := rec
(
  isRWS := true,
  generatorOrder := [a,b,c],
  ordering := "shortlex",
  inverses := [a,b,c],
  equations := 
  [
    [c*a*c*a,IdWord],
    [b*a*b*a*b*a*b*a*b*a,IdWord],
    [c*b*c*b*c*b*c*b*c*b,IdWord]
  ]
);
