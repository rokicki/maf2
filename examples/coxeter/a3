# Coxeter graph
#  a-b-c
#    
#    
# Coxeter Matrix (above upper diagonal)
# 1 3 2  
#   1 3  
#     1  
#
#Coxeter group A3: Symmetry group of tetrahedron
_RWS := rec
(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,b,c],
        inverses := [a,b,c],
  equations :=
  [
    [b*a*b,a*b*a],
    [c*a,a*c],
    [c*b*c,b*c*b],
  ]
);
