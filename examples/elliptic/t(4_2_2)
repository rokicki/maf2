_RWS := rec
(
  isRWS := true,
  generatorOrder := [a,b,c],
  ordering := "shortlex",
  inverses := [a,b,c],
  equations := 
  [
    [(b*c)^4,IdWord],
    [(c*a)^2,IdWord],
    [(a*b)^2,IdWord]
  ]
);
